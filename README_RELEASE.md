# Star Fire CRM - Release

Este arquivo foi consolidado.

Use o playbook oficial em:

- `docs/RELEASE_PLAYBOOK.md`

Atalho rapido:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\Publish-StarFire.ps1 -Version <versao> -BuildInstaller
```
